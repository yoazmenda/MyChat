server:
  port: '8443'

async:
    corePoolSize: '2'
    maxPoolSize: '50'
    queueCapacity: '10000'

console:
    oauth:
       clientid: 'morph'
       secret: '1234'
       tokenValidityInSeconds: '1800'
    jwt:
        keyPath: '${certificate.path.jwt:${user.dir}/../certificate/keystore.jks}'
        pass: 'PWL8N6ORVNNL'
        pair: 'jwt'
        expired: '1800'
    logging:
        logstash:
            enabled: 'false'
            host: 'localhost'
            port: '3456'
            connectionTimeout: '10000'
            reconnectionDelay: '2000'